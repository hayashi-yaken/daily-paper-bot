# DPB-006: 通知サービス（Slack/Discord）

## 基本情報

| 項目       | 内容                          |
| ---------- | ----------------------------- |
| タスクID   | DPB-006                       |
| タスク名   | 通知サービス（Slack/Discord） |
| Phase      | Phase 1: コア機能             |
| 優先度     | 必須                          |
| ステータス | 未着手                        |
| 関連機能ID | F-09 (仕様書 Sec. 9)          |
| 関連画面ID | -                             |

## 概要

選定された論文の情報を整形し、指定されたプラットフォーム（SlackまたはDiscord）に投稿する機能を実装します。

## やること

- [ ] `internal/formatter` パッケージで、論文情報から投稿用メッセージを生成する機能を実装する。
- [ ] `internal/notifier` パッケージを作成する。
- [ ] `Notifier` インターフェースと、それを実装した `SlackNotifier` と `DiscordNotifier` を作成する。
- [ ] `SlackNotifier` は、Slack APIクライアントライブラリを使ってメッセージを投稿する。
- [ ] `DiscordNotifier` は、Webhook URLに対してHTTP POSTリクエストを送信してメッセージを投稿する。
- [ ] Abstractが長い場合に指定文字数で切り詰める処理を実装する。
- [ ] 投稿失敗時のエラーハンドリングを実装する。

## 方針

- **Formatter**: 論文データを受け取り、仕様書通りのMarkdown文字列を返す責務を持つ。
- **Notifier**: Formatterが生成した文字列を受け取り、各プラットフォームへ通知する責務を持つ。
- **Slack**: `slack-go/slack` などの実績あるライブラリを利用する。
- **Discord**: 標準の `net/http` でWebhookにPOSTするシンプルな実装とする。

## 方法

### 1. Formatterの実装

```go
// internal/formatter/formatter.go
package formatter

import (
	"daily-paper-bot/internal/openreview"
	"fmt"
	"strings"
)

func FormatPaper(paper *openreview.Paper, maxChars int) string {
	abstract := paper.Abstract
	if len(abstract) > maxChars {
		abstract = string([]rune(abstract)[:maxChars]) + "..."
	}

	// ... 投稿メッセージを組み立てる ...

	return fmt.Sprintf(
		"📄 今日の論文 (%s %d)\n\nTitle: %s\nAuthors: %s\n\nAbstract:\n%s\n\nLink:\n%s\nID: %s",
		paper.Venue, paper.Year, paper.Title, strings.Join(paper.Authors, ", "), abstract, paper.PDFURL, paper.ID,
	)
}
```

### 2. Notifierの実装

```go
// internal/notifier/notifier.go
package notifier

// Notifier はメッセージを通知する
type Notifier interface {
	Post(message string) error
}

// --- Slack ---
// internal/notifier/slack.go
import "github.com/slack-go/slack"

type SlackNotifier struct {
	client    *slack.Client
	channelID string
}
// ... NewSlackNotifier, Post ...

// --- Discord ---
// internal/notifier/discord.go
import "net/http"

type DiscordNotifier struct {
	webhookURL string
	httpClient *http.Client
}
// ... NewDiscordNotifier, Post ...
```

## 成果物

- [ ] `internal/formatter` パッケージ
- [ ] `internal/notifier` パッケージ（Slack/Discord実装を含む）
- [ ] ユニットテスト

## 依存タスク

- DPB-001: プロジェクト初期設定と基本構造

## 後続タask

- DPB-007: メインプロセスと実行フローの統括
